---
import { getVisibleEssays } from "../../lib/essays";

const hasEssays = (await getVisibleEssays()).length > 0;

if (hasEssays) return Astro.redirect(`${import.meta.env.BASE_URL}#mosaico`);
return Astro.redirect(import.meta.env.BASE_URL);
---
